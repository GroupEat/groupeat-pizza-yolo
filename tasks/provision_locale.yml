---
- name: Set locale
  lineinfile: dest=/etc/environment line="LC_ALL=\"en_US.UTF-8\"" state=present
  register: locale

- name: Reboot server
  command: shutdown -r now "Locale changed"
  when: locale.changed
  async: 0
  poll: 0
  ignore_errors: true

- name: Wait for server to come back
  local_action: wait_for host={{ ansible_ssh_host }} port=22 delay=1 timeout=300 state=started
  when: locale.changed
  sudo: false
